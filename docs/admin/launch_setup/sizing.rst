.. _sizing:

Типовой сайзинг
================

Раздел содержит типовой расчёт инфраструктуры для развёртывания Citeck. На этой странице собрано то, что **одинаково для всех вариантов развёртывания**: целевая нагрузка, состав и ресурсы контейнеров платформы, правила пересчёта с учётом фактического объёма документооборота и границы применимости. Детальный расчёт нод и виртуальных машин вынесен в подразделы по способу развёртывания; здесь приведены сравнение способов и сводка итоговых конфигураций.

.. note::

    * Расчёт выполнен для **2000 именных пользователей** и применим при любом их числе до этого значения.
    * Ресурсы контейнеров и подов взяты из раздела :ref:`Менеджмент ресурсов подов и контейнеров <k8s-resource-management>`.
    * Эти ресурсы подтверждены нагрузочным тестированием в Kubernetes: **1000 одновременных пользователей** в течение 1 часа (~2 млн запросов, средний отклик 6.88 мс, 99-й перцентиль 47 мс). Подробнее — :ref:`Результаты нагрузочного тестирования <load_testing>`.

.. contents::
    :depth: 2

Подразделы
-----------

.. grid:: 1
   :gutter: 3

   .. grid-item-card:: Сайзинг в Kubernetes
      :link: sizing_k8s
      :link-type: ref

      Worker-ноды продуктивного кластера и кластера для разработки и тестирования (**Test** или **Dev + Test**): типоразмер ноды, количество нод с резервированием N+1, Persistent Volumes и диски вне кластера.

   .. grid-item-card:: Сайзинг на одной ВМ: Citeck Launcher
      :link: sizing_docker
      :link-type: ref

      Конфигурации ВМ для развёртывания с помощью Citeck Launcher 2.x — профили **до 100** и **до 2000 именных пользователей**, разметка дисков, ограничения схемы с одной ВМ.

   .. grid-item-card:: Требования к внешним сервисам
      :link: sizing_external
      :link-type: ref

      PostgreSQL, S3-совместимое хранилище, OpenSearch и RabbitMQ при выносе за пределы кластера или ВМ: ресурсы, диски, версии, перечень баз данных.

Целевая нагрузка
-----------------

.. list-table::
   :header-rows: 1
   :widths: 35 65
   :class: tight-table

   * - Параметр
     - Значение
   * - Число именных пользователей
     - **2000**
   * - Число одновременных пользователей в пике
     - до 500 (соотношение именных к одновременным — **4:1**)
   * - Подтверждение нагрузочным тестированием
     - 1000 одновременных пользователей в течение 1 часа: ~2 млн запросов, средний отклик 6.88 мс
   * - Профиль работы
     - типовой документооборот: карточки, маршруты согласования, поиск, работа с вложениями
   * - Базовый объём контента
     - 500 ГБ

Соотношение именных и одновременных пользователей **4:1** принято во всех расчётах раздела, включая подразделы.

Выбор способа развёртывания
----------------------------

.. important::

    Вне Kubernetes единственный рекомендуемый способ развёртывания — Citeck Launcher 2.x в :ref:`серверном режиме <launcher_server>`. Docker Compose **больше не поддерживается**: конфигурации Docker Compose для новых версий Citeck выпускаться не будут. Перенос существующей установки под управление лончера — :ref:`migration_from_compose`.

.. list-table::
   :header-rows: 1
   :widths: 22 39 39
   :class: tight-table

   * - Аспект
     - Одна ВМ (Citeck Launcher 2.x)
     - Kubernetes
   * - Отказоустойчивость
     - отсутствует: ВМ — единая точка отказа
     - резерв N+1 по worker-нодам, перезапуск подов на другой ноде; PostgreSQL и S3-хранилище в базовом расчёте развёрнуты в одном экземпляре
   * - Обновления
     - с остановкой сервисов
     - поэтапное обновление (rolling update); при нескольких репликах сервисов — без деградации
   * - Масштабирование
     - вертикальное — за счёт увеличения ресурсов ВМ. Тиражирование отдельных микросервисов в Citeck Launcher пока не поддерживается: оно появится в будущих версиях, но только в пределах одной ВМ
     - горизонтальное — за счёт добавления нод и реплик штатными средствами кластера
   * - Кластеризация PostgreSQL, S3, OpenSearch
     - в рамках одной ВМ не предусмотрена, требует выноса компонентов на отдельные ВМ
     - обеспечивается выносом компонентов на выделенные ВМ или в managed-сервисы (в расчёт не входит)
   * - Область применения
     - демо- и тестовые контуры, продуктив без формальных требований к доступности
     - продуктив с требованиями к доступности

.. tip::

    Развёртывание на одной ВМ уместно для тестовых и демонстрационных контуров, а также для продуктива без формальных требований к доступности. При наличии требований к допустимой потере данных и времени восстановления (RPO / RTO) либо при численности свыше 2000 именных пользователей рекомендуется :ref:`развёртывание в Kubernetes <sizing_k8s>`.

Итоговые конфигурации
~~~~~~~~~~~~~~~~~~~~~~

Сводка по обоим способам развёртывания; детали расчёта — в подразделах.

.. list-table::
   :header-rows: 1
   :widths: 22 20 29 29
   :class: tight-table

   * - Способ развёртывания
     - Контур
     - Без полнотекстового поиска
     - С полнотекстовым поиском (OpenSearch)
   * - :ref:`Одна ВМ <sizing_docker>` — до 100 именных
     - Prod
     - 8–16 vCPU / 24–32 GiB
     - 8–16 vCPU / 32–40 GiB
   * - :ref:`Одна ВМ <sizing_docker>` — до 2000 именных
     - Prod
     - 16 vCPU / 48 GiB
     - 24 vCPU / 64 GiB
   * - :ref:`Одна ВМ <sizing_docker>` — до 2000 именных
     - Test
     - 16 vCPU / 48 GiB
     - 16 vCPU / 48 GiB
   * - :ref:`Kubernetes <sizing_k8s>` — до 2000 именных
     - Prod
     - 2 × (16 vCPU / 32 GiB) worker-ноды + :ref:`внешние сервисы <sizing_external>`
     - то же + узел OpenSearch
   * - :ref:`Kubernetes <sizing_k8s>` — до 2000 именных
     - Test
     - 1–2 × (16 vCPU / 32 GiB) worker-ноды + :ref:`внешние сервисы <sizing_external>`
     - то же + узел OpenSearch
   * - :ref:`Kubernetes <sizing_k8s>` — до 2000 именных
     - Dev + Test
     - 2–3 × (16 vCPU / 32 GiB) worker-ноды + :ref:`внешние сервисы <sizing_external>`
     - то же + узел OpenSearch

Что определяет объём ресурсов
------------------------------

Расчёт описывает **минимальный объём ресурсов платформы**: состав микросервисов задан поставкой Citeck и при уменьшении числа пользователей не сокращается. При 500 именных пользователях и том же объёме документооборота конфигурация остаётся такой же, как при 2000, — меняется только объём логов. Профиль до 100 именных пользователей на одной ВМ компактнее за счёт меньших ресурсов PostgreSQL и меньшего запаса на пики (см. :ref:`sizing_docker`).

Основной фактор, определяющий потребность в ресурсах, — **объём документооборота**, а не число пользователей: от него зависят объём контента в S3, дисковое пространство и ресурсы PostgreSQL, а также размер поисковых индексов.

.. _sizing_recalc:

Правила пересчёта при изменении объёма документооборота
--------------------------------------------------------

Правила общие для всех вариантов развёртывания.

* **Контент** — суммарный объём файлов; базовое допущение расчёта — **500 ГБ** на продуктиве.
* **База данных PostgreSQL** ≈ 0.6 × объёма контента плюс ~30 % от этого значения на WAL, временные файлы и служебные данные (~400 ГБ при 500 ГБ контента).
* **Поисковые индексы OpenSearch** ≈ 0.5 × объёма контента (250 ГБ при 500 ГБ контента). При переходе на отказоустойчивую конфигурацию с репликами индексов объём возрастает кратно числу копий индекса.
* **Логи** ≈ 1 ГБ на 500 именных пользователей в год.
* Резервное копирование и место под резервные копии в настоящем расчёте не рассматриваются.

**Ресурсы PostgreSQL в зависимости от объёма БД:**

.. list-table::
   :header-rows: 1
   :widths: 50 50
   :class: tight-table

   * - Объём базы данных
     - Ресурсы выделенного сервера СУБД
   * - ≤ 100 ГБ
     - 4 vCPU / 16 GiB
   * - ≤ 1 ТБ
     - 8 vCPU / 32 GiB
   * - ≤ 10 ТБ
     - 16 vCPU / 64 GiB
   * - ≤ 50 ТБ
     - 24 vCPU / 128 GiB

.. note::

    Значения рассчитаны на **выделенный сервер СУБД**. При размещении PostgreSQL на общей ВМ вместе с контейнерами платформы ресурсы берутся из предыдущей строки таблицы (для базы до 1 ТБ — 4 vCPU / 16 GiB вместо 8 vCPU / 32 GiB; для базы до 100 ГБ — 2 vCPU / 4 GiB вместо 4 vCPU / 16 GiB): процессорные ядра и файловый кэш используются совместно со всей платформой.

**Ориентир по OpenSearch:** ~1 GiB heap на 20–30 ГБ индексов, heap не более 31 GiB и не более 50 % RAM узла.

Когда расчёт требует пересмотра
--------------------------------

.. list-table::
   :header-rows: 1
   :widths: 40 60
   :class: tight-table

   * - Условие
     - Что меняется
   * - Свыше 2000 именных (более 500 одновременных) пользователей
     - тиражирование Ecos Model, Gateway, Process; в Kubernetes — добавление worker-нод. На одной ВМ тиражирование микросервисов в Citeck Launcher пока не поддерживается (появится в будущих версиях, в пределах одной ВМ) — рекомендуется переход на Kubernetes
   * - Объём БД свыше 1 ТБ
     - увеличение ресурсов PostgreSQL (CPU и RAM) по таблице выше
   * - Объём контента свыше 2–3 ТБ
     - вынос СУБД и S3-хранилища на выделенные ВМ или в managed-сервисы; переход на кластерное S3-совместимое хранилище (см. :ref:`sizing_s3`); развёртывание OpenSearch на нескольких узлах
   * - Требования к RPO / RTO
     - кластеризация PostgreSQL, S3 и OpenSearch — в настоящем расчёте не учтена. Схема с одной ВМ отказоустойчивости не обеспечивает: требуется переход на :ref:`Kubernetes <sizing_k8s>`

Методика расчёта
-----------------

* **Requests** — гарантированный минимум, по нему планируется потребность в CPU.
* **Limits** — верхняя граница потребления. Потребность в памяти планируется по ``limits``, так как ``limits`` ≈ ``requests``: запаса на кратковременные пики практически нет.
* Значения в таблице ресурсов, кроме S3-хранилища, приведены по документации Citeck (:ref:`k8s-resource-management`). В Kubernetes они задаются в секции ``resources``, а в Citeck Launcher лимит памяти контейнера и размер heap JVM задаются параметрами приложения ``memoryLimit`` и ``heapSize`` соответственно (см. :ref:`Конфигурация серверного режима <server_configuration>`).
* Сумма ``limits`` по CPU показывает верхнюю границу одновременного потребления и на практике не достигается, поэтому потребность в CPU планируется по ``requests`` с запасом, заложенным в рекомендуемых конфигурациях.
* 1 Gi (GiB) = 1024 Mi (MiB); 1 vCPU = 1000m, то есть 100m — это 0.1 vCPU.

.. _sizing_containers:

Состав и ресурсы контейнеров платформы
---------------------------------------

Таблица одинакова для всех способов развёртывания и любого числа пользователей: состав контейнеров задан поставкой Citeck.

.. list-table::
   :header-rows: 1
   :widths: 32 17 17 17 17
   :class: tight-table

   * - Контейнер / микросервис
     - Requests CPU
     - Requests MEM
     - Limits CPU
     - Limits MEM
   * - Ecos Model
     - 2
     - 1 Gi
     - 2
     - 1 Gi
   * - Ecos Gateway
     - 2
     - 1 Gi
     - 2
     - 1 Gi
   * - Ecos Apps
     - 100m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Process
     - 1
     - 3548 Mi
     - 1
     - 3548 Mi
   * - Ecos Uiserv
     - 1
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos History
     - 100m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Integrations
     - 100m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Notifications
     - 100m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Transformations
     - 100m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Ecom
     - 100m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Service Desk
     - 100m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Edi
     - 100m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Content
     - 100m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Proxy
     - 300m
     - 256 Mi
     - 1
     - 512 Mi
   * - Zookeeper
     - 500m
     - 1 Gi
     - 1
     - 1 Gi
   * - Ecos Identity (Keycloak)
     - 300m
     - 1 Gi
     - 1
     - 1 Gi
   * - Onlyoffice
     - 100m
     - 2 Gi
     - 2
     - 2 Gi
   * - RabbitMQ
     - 500m
     - 1 Gi
     - 1
     - 1 Gi
   * - S3-хранилище (1 узел)
     - 1
     - 2 Gi
     - 2
     - 4 Gi
   * - **Итого по контейнерам платформы**
     - **9.60 vCPU**
     - **22.71 Gi**
     - **23.00 vCPU**
     - **24.96 Gi**

.. seealso::

    * :ref:`Менеджмент ресурсов подов и контейнеров <k8s-resource-management>` — исходные значения ``resources`` и параметры JVM для каждого микросервиса.
    * :ref:`Сервисы Docker <docker_services>` — назначение образов и переменные окружения.
    * :ref:`Архитектура <architecture>` — состав микросервисов, зависимости компонентов и схема хранения данных.

Что не входит в расчёт
-----------------------

* **Кластеризация и отказоустойчивость PostgreSQL** — репликация, Patroni, etcd, балансировщик подключений.
* **Кластеризация и отказоустойчивость S3-хранилища** — многоузловое развёртывание, erasure coding, георезервирование.
* **Кластеризация OpenSearch** — кворум из трёх узлов, реплики индексов.
* **Компоненты искусственного интеллекта** — LLM (генеративные модели), ASR-модели для транскрибации аудио и видео (GigaAM, Whisper и аналоги), embedding-модели и векторное хранилище для RAG. Требуют отдельных GPU-ресурсов либо внешних inference-сервисов; состав компонентов — см. :ref:`Механика выполнения AI-агентов <ai-architecture>`, :ref:`Настройка семантического поиска <rag-config>` и :ref:`Модуль записи совещаний <call-recording-module>`.
* **Резервное копирование** и место под резервные копии.
* **Инструменты CI/CD**.
* **Мониторинг и сбор логов** — учтены только в виде накладных расходов агентов на ноде или ВМ. Инструменты наблюдения за платформой — см. :ref:`Spring Boot Admin <spring_boot_admin>`.
* **Управляющие компоненты Kubernetes-кластера** (control plane / master-ноды).

.. toctree::
    :maxdepth: 2
    :hidden:

    sizing/kubernetes
    sizing/docker
    sizing/external_services
